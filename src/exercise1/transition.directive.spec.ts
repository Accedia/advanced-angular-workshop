import { TransitionDirective } from './transition.directive';

describe('TransitionDirective', () => {
  it('should create an instance', () => {
    const directive = new TransitionDirective();
    expect(directive).toBeTruthy();
  });
});
